import { Box } from "@mui/material";

export const MainImageSection: React.FC = () => {
  return (
    <Box
      sx={{
        width: "800px",
        height: "300px",
        backgroundColor: "#E0E0E0",
        borderRadius: 6,
      }}
    />
  );
};
